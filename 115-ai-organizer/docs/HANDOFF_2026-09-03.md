# 115 AI 文件整理系统｜2026-09-03 交接文档

交接日期：2026-09-03（在公司电脑完成，回家电脑恢复用）

代码仓库：`https://github.com/yaoy2/yao_1`

项目目录：`115-ai-organizer/`

当前版本提交：`d46d873 feat(115-ai-organizer): series clustering, movie-by-title, time fallback, web pagination`

## 1. 今天做了什么

1. **整理目标从“云下载”切换为“星标视频”**（CID `3508627805157983969`）：
   - OpenList 存储（115 Open 驱动）：根文件夹 ID、挂载路径（`/星标视频`）已改，存储 ID 仍是 1。
   - 只读账号 `organizer-readonly`（用户 ID 3）：基本路径已改为 `/星标视频`，权限仍为只读（1）。
   - 项目 `.env`：`OPENLIST_MOUNT_PATH`、`ALLOWED_ROOT`、`DEFAULT_SCAN_DIR` 都改为 `/星标视频`。
2. **115 授权重新做过一次**：早上旧 refresh_token 失效（错误码 40140120），Sir 在 <https://api.oplist.org/> 重新获取 Token 并在 OpenList 管理页更新，之后状态 `work`。
3. **完整只读扫描**：422 目录 / 8860 文件 / 约 277GB，原生 ID 全覆盖，无重复风险组，未修改 115 任何内容。
4. **新增整理规则**（详见 README“整理规则”一节和 CHANGELOG）：
   - 系列归集 `/系列/<系列名>/`（不改文件名）。
   - 电影 `/电影/<片名 (年份)>/`。
   - 待识别按添加时间月兜底 `/按时间归集/YYYY-MM/`。
   - 图片文件名能对上视频系列的跟随系列，否则留在 `/图片/`。
5. **网页分页**：计划列表每页 50/200/1000 条，修复一次渲染 8785 条导致骨架屏数分钟的问题。
6. 测试：52 项全部通过；改动已提交并推送（`d46d873`）。

## 2. 回家后按顺序恢复

### 2.1 拉代码

```powershell
git -C E:\github\yao_1 pull
git -C E:\github\yao_1 log -1 --oneline   # 应看到 d46d873 或更新
```

如果家里还没克隆：`git clone https://github.com/yaoy2/yao_1.git E:\github\yao_1`

### 2.2 安装依赖并跑测试

```powershell
cd E:\github\yao_1\115-ai-organizer
python -m pip install -r requirements.txt
python -m unittest discover -s tests -q   # 应 52 项全部通过
```

### 2.3 恢复 OpenList

```powershell
# 从 Google Drive 同步的副本复制（如果 E:\OpenList 不存在）
Copy-Item -Recurse E:\GoogleDrive\Ding2026\OpenList E:\OpenList

E:\OpenList\openlist.exe start
Invoke-WebRequest http://127.0.0.1:5244/ping   # 应返回 200
```

注意：今天改过存储配置（根 CID 切到星标视频），配置存在 `E:\OpenList\data\data.db` 里。
**如果 Google Drive 同步的是旧副本，data.db 里可能还是“云下载”的旧 CID（`749127585111375226`）**，需要确认/修改：

- 打开 `http://127.0.0.1:5244` → 管理（admin，密码在 `E:\OpenList\ADMIN_PASSWORD.txt`）→ 存储。
- 根文件夹 ID 应为 `3508627805157983969`，挂载路径 `/星标视频`。不对就编辑改过来。

### 2.4 检查 / 重新做 115 授权

Token 可能在家里的这段时间过期：

```powershell
cd E:\github\yao_1\115-ai-organizer
python -m app status        # logged_in 应为 true
python -m app probe --dir "/星标视频"   # 应能列出内容，不报 refresh_token 无效
```

如果报 40140120（refresh_token 无效）：到 <https://api.oplist.org/> 重新获取 Token（勾选“使用 OpenList 提供的参数”，Client ID/秘钥留空），在 OpenList 管理页 → 存储 → 编辑，只更新 Access Token 和 Refresh Token。

### 2.5 恢复 .env

`.env` 不在 Git 里。从 Ding2026 副本复制后**必须检查三项**（今天改过，旧副本里还是 /云下载）：

```text
OPENLIST_MOUNT_PATH=/星标视频
ALLOWED_ROOT=/星标视频
DEFAULT_SCAN_DIR=/星标视频
WRITE_MODE=false
```

### 2.6 重新扫描（推荐，不复制旧数据库）

本地索引/报告不会随 Git 走，到家重新扫一遍更可靠：

```powershell
# 注意 Git Bash 里 --dir 要写 "//星标视频" 防止路径转义；PowerShell 无此问题
python -m app full-workflow --root-folder-id "3508627805157983969" --dir "/星标视频" --depth 8 --max-files 0
```

本地 SQLite（`data\115_index.sqlite`）如果在公司电脑上，也可以从 Ding2026 复制，但重扫更准。

### 2.7 启动网页

```powershell
# 方式一：双击 scripts\start_web.bat（别关黑窗口）
# 方式二：后台运行
python -m streamlit run app/web.py --server.headless true --server.port 8502
```

浏览器打开 `http://localhost:8502`。现在有分页，几秒内应渲染完成；
如果还是骨架屏，等一次完整加载后 F5。

## 3. 接下来的工作（按优先级）

1. **审核整理建议**：网页里按分类筛选查看。系列 231 项里有泛用名组（如 `/系列/new 9`、`/系列/IMG`），不认可的逐条取消批准。
2. **第一次真实执行**：建议先只批准 1 个系列组或 1 部电影，走完整流程：
   `python -m app prepare-execution --scan-root-id "3508627805157983969" --scan-root-path "/星标视频"`
   → 核对清单 → `python -m app execute-open115 --manifest 清单.json --confirm "APPLY-确认码"` → 115 网页人工复核。
3. **可选增强**（讨论过未实现）：
   - 图片按来源目录结构归集（当前只有跟随系列的 11 张有去处）。
   - 用视频时长（已入库）区分长片/短片段分开归档。
   - 清理电影分类里的垃圾推广长文件名（当前会被 `(2048)` 等伪年份骗到）。

## 4. 遗留问题 / 注意事项

- OpenList Token 失效过一次（40140120），家里大概率要重新授权一次。
- “批准筛选结果”按钮批准的是**当前筛选下的全部条目**（跨页），不是只批准当前页。
- 整理期间不要再增删“星标视频”里的文件，否则扫描快照失效需重扫。
- 不要把根目录仓库里 docker-monitor 相关的未提交本地改动混进 115 项目提交（公司电脑工作区里还留着一堆，未提交）。
- 仓库里其他已修改未提交文件（根 README/CHANGELOG/hello.py 等）是别的用途的本地改动，与本项目无关。

## 5. 常用入口（同前）

```text
完整扫描与报告：scripts\full_scan_and_report.bat
查看网页：        scripts\start_web.bat
打开最新报告：    scripts\open_latest_report.bat
生成操作清单：    scripts\prepare_organize.bat
执行审核清单：    scripts\execute_reviewed_manifest.bat
完整说明：        README.md
安全设计：        docs\full-workflow.md
上一份交接：      docs\HANDOFF_2026-09-02.md
```
