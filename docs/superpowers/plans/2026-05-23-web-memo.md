# Web Memo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Streamlit page for saving online quotes, jokes, and reusable wording with card-style display and export.

**Architecture:** Keep persistence and export logic in `utils/web_memo_db.py`. Keep the Streamlit page thin in `pages/10_10、🧾_灵感便签盒.py`. Use two-digit hidden sort prefixes plus visible numbering, such as `01_1、...`, so Pages sort correctly and Streamlit still shows module numbers. Reuse `data/color_palettes.md` for card color cycling.

**Tech Stack:** Python, Streamlit, SQLite, unittest.

---

### Task 1: Data Layer

**Files:**
- Create: `utils/web_memo_db.py`
- Test: `tests/test_web_memo_db.py`

- [ ] Write tests for save, list, classify, Markdown export, and palette cycling.
- [ ] Run `python -m unittest tests.test_web_memo_db -v` and confirm the missing module failure.
- [ ] Implement SQLite data functions and local rule classification.
- [ ] Run the same test and confirm it passes.

### Task 2: Streamlit Page

**Files:**
- Create: `pages/10_10、🧾_灵感便签盒.py`

- [ ] Build a two-column page matching the approved v3 mockup.
- [ ] Use the subtitle “我来监督沙瑞金”.
- [ ] Use textarea placeholder “请输入”.
- [ ] Render memo cards with colors from `data/color_palettes.md`.
- [ ] Add Markdown and HTML download buttons.

### Task 3: Verification

**Files:**
- Existing tests plus new page.

- [ ] Run `python -m unittest tests.test_web_memo_db -v`.
- [ ] Run the broader available unit tests if the environment can import page modules.
- [ ] Launch Streamlit and visually verify the page loads.
