# M20 Ding2026 Read-Only Showcase Implementation Plan

> Status (2026-08-31): M20 is implemented. M21 and the section-based homepage were added later the same day, so the 19-module, M20-first, and 9-card pagination assertions below are retained as historical implementation context rather than current repository requirements. Use `hello.py`, the READMEs, and `docs/repository-structure.md` for the current state.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add M20 to the YaoYao Streamlit toolbox as a clear, read-only explanation of the independent Ding2026 file-governance project, including its purpose, capabilities, local workflow, archive model, safety boundaries, and a desensitized status snapshot.

**Architecture:** Keep all operational Ding2026 code in the independent `E:\github\ding2026-system` repository. In `yao_1`, add one Streamlit page under `pages/`, a strict loader under `utils/`, and one static JSON snapshot under `assets/`; register the page through the existing `hello.py` source of truth so the current sidebar derives it automatically.

**Tech Stack:** Python 3.11, Streamlit 1.57, standard-library JSON/path handling, pytest, Streamlit `AppTest`.

**Spec:** `docs/superpowers/specs/2026-08-31-m20-ding2026-showcase-design.md`

## Global Constraints

- The page is display-only: no archive, distribute, undo, configure, refresh, upload, download, local-console, database, directory-scan, or network action.
- `E:\github\ding2026-system` remains the sole operational source repository and must not be modified during implementation.
- The public snapshot contains only the ten fields approved by the spec and no names, filenames, absolute paths, credentials, or configuration values.
- Do not run `streamlit run`; verify the page with `streamlit.testing.v1.AppTest`, tests, syntax checks, and the deployed Streamlit URL after push.
- Preserve unrelated user changes. Do not rewrite history, force-push, or delete cleanup candidates without a separate post-report confirmation.
- Keep `README.md == README_EN.md` and `CHANGELOG.md == CHANGELOG_EN.md`.

---

### Task 1: Public Snapshot Contract

**Files:**
- Create: `utils/ding2026_showcase.py`
- Create: `tests/test_ding2026_showcase_page.py`

**Interfaces:**
- Produces: `load_public_snapshot(path: str | Path) -> dict[str, str | int] | None`
- Produces: `SNAPSHOT_FIELDS`, `STRING_FIELDS`, and `COUNT_FIELDS` as immutable field sets/tuples used by tests and the page.
- Consumes: UTF-8 JSON stored at a caller-supplied path.

- [ ] **Step 1: Write failing loader tests**

Create `tests/test_ding2026_showcase_page.py` with literal fixtures that exercise the real loader:

```python
import json
from pathlib import Path

from utils.ding2026_showcase import SNAPSHOT_FIELDS, load_public_snapshot


VALID_SNAPSHOT = {
    "schema_version": "1",
    "project_version": "v1.1.0",
    "generated_at": "2026-08-31 00:55",
    "logical_files": 4293,
    "instances": 6721,
    "pending": 714,
    "invoices": 60,
    "official_docs": 96,
    "operations": 2920,
    "root_left": 0,
}


def write_snapshot(path: Path, payload: object) -> None:
    path.write_text(json.dumps(payload, ensure_ascii=False), encoding="utf-8")


def test_load_public_snapshot_accepts_only_the_desensitized_aggregate(tmp_path):
    path = tmp_path / "snapshot.json"
    write_snapshot(path, VALID_SNAPSHOT)

    assert load_public_snapshot(path) == VALID_SNAPSHOT
    assert set(VALID_SNAPSHOT) == SNAPSHOT_FIELDS


def test_load_public_snapshot_rejects_an_unapproved_field(tmp_path):
    path = tmp_path / "snapshot.json"
    write_snapshot(path, {**VALID_SNAPSHOT, "root_path": "sensitive"})

    assert load_public_snapshot(path) is None


def test_load_public_snapshot_rejects_invalid_counts_and_strings(tmp_path):
    path = tmp_path / "snapshot.json"
    invalid_payloads = [
        {**VALID_SNAPSHOT, "pending": -1},
        {**VALID_SNAPSHOT, "operations": True},
        {**VALID_SNAPSHOT, "generated_at": ""},
    ]

    for payload in invalid_payloads:
        write_snapshot(path, payload)
        assert load_public_snapshot(path) is None


def test_load_public_snapshot_falls_back_for_missing_or_malformed_files(tmp_path):
    missing = tmp_path / "missing.json"
    malformed = tmp_path / "malformed.json"
    malformed.write_text("{", encoding="utf-8")

    assert load_public_snapshot(missing) is None
    assert load_public_snapshot(malformed) is None
```

The production breaks these tests if it accepts private fields, negative/boolean counters, blank metadata, malformed JSON, or missing files as a valid snapshot.

- [ ] **Step 2: Run the tests and verify RED**

Run:

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_ding2026_showcase_page.py -q
```

Expected: collection fails with `ModuleNotFoundError: No module named 'utils.ding2026_showcase'`.

- [ ] **Step 3: Implement the strict loader**

Create `utils/ding2026_showcase.py`:

```python
"""Public-data boundary for the M20 Ding2026 showcase."""

from __future__ import annotations

import json
from pathlib import Path


STRING_FIELDS = ("schema_version", "project_version", "generated_at")
COUNT_FIELDS = (
    "logical_files",
    "instances",
    "pending",
    "invoices",
    "official_docs",
    "operations",
    "root_left",
)
SNAPSHOT_FIELDS = frozenset((*STRING_FIELDS, *COUNT_FIELDS))


def load_public_snapshot(path: str | Path) -> dict[str, str | int] | None:
    try:
        payload = json.loads(Path(path).read_text(encoding="utf-8"))
    except (OSError, UnicodeError, json.JSONDecodeError):
        return None

    if not isinstance(payload, dict) or set(payload) != SNAPSHOT_FIELDS:
        return None
    if any(not isinstance(payload[name], str) or not payload[name].strip() for name in STRING_FIELDS):
        return None
    for name in COUNT_FIELDS:
        value = payload[name]
        if isinstance(value, bool) or not isinstance(value, int) or value < 0:
            return None
    return payload
```

- [ ] **Step 4: Run the loader tests and verify GREEN**

Run the same targeted pytest command. Expected: `4 passed` with no warnings from the new module.

- [ ] **Step 5: Commit the loader contract**

```powershell
git add -- utils/ding2026_showcase.py tests/test_ding2026_showcase_page.py
git diff --cached --check
git commit -m "test: define Ding2026 public snapshot boundary"
```

---

### Task 2: Static Snapshot and Read-Only M20 Page

**Files:**
- Create: `assets/ding2026_m20_snapshot.json`
- Create: `pages/19_20_ding2026.py`
- Modify: `tests/test_ding2026_showcase_page.py`

**Interfaces:**
- Consumes: `load_public_snapshot(ROOT / "assets" / "ding2026_m20_snapshot.json")`.
- Produces: Streamlit page title `M20 · Ding2026 文件治理系统`, compact aggregate metrics, and five explanatory tabs.
- Produces: no operational widget or operational import.

- [ ] **Step 1: Add failing page-behavior and read-only policy tests**

Append these tests:

```python
import ast

from streamlit.testing.v1 import AppTest


ROOT = Path(__file__).resolve().parents[1]
PAGE = ROOT / "pages" / "19_20_ding2026.py"
SNAPSHOT = ROOT / "assets" / "ding2026_m20_snapshot.json"


def test_repository_snapshot_uses_the_public_contract():
    assert load_public_snapshot(SNAPSHOT) == VALID_SNAPSHOT


def test_m20_page_explains_purpose_capabilities_and_local_use():
    app = AppTest.from_file(str(PAGE), default_timeout=10)
    app.run()

    assert not app.exception
    assert app.title[0].value == "M20 · Ding2026 文件治理系统"
    visible_text = "\n".join(
        item.value
        for collection in (app.markdown, app.info, app.warning, app.caption, app.subheader)
        for item in collection
    )
    for phrase in ("本页仅展示", "它能做什么", "日常怎么用", "五条时间轴", "永不自动删除"):
        assert phrase in visible_text
    assert len(app.metric) >= 5


def test_m20_page_has_no_operational_controls_or_runtime_dependencies():
    tree = ast.parse(PAGE.read_text(encoding="utf-8"))
    forbidden_calls = {
        "button",
        "download_button",
        "file_uploader",
        "form",
        "form_submit_button",
        "link_button",
        "switch_page",
    }
    streamlit_calls = {
        node.func.attr
        for node in ast.walk(tree)
        if isinstance(node, ast.Call)
        and isinstance(node.func, ast.Attribute)
        and isinstance(node.func.value, ast.Name)
        and node.func.value.id == "st"
    }
    assert streamlit_calls.isdisjoint(forbidden_calls)

    imported_modules = {
        alias.name
        for node in ast.walk(tree)
        if isinstance(node, ast.Import)
        for alias in node.names
    } | {
        node.module or ""
        for node in ast.walk(tree)
        if isinstance(node, ast.ImportFrom)
    }
    assert not any(name == "ding2026" or name.startswith("ding2026.") for name in imported_modules)
    source = PAGE.read_text(encoding="utf-8")
    assert "127.0.0.1" not in source
    assert "E:\\github\\ding2026-system" not in source
```

The AppTest fails if the page does not render its public explanation. The AST policy test fails if a later change adds an operational Streamlit control, imports the Ding2026 runtime, or embeds a local control URL/path.

- [ ] **Step 2: Run the expanded test and verify RED**

Run the targeted test. Expected: failures because the page and repository snapshot do not exist.

- [ ] **Step 3: Add the approved static snapshot**

Create `assets/ding2026_m20_snapshot.json` with exactly:

```json
{
  "schema_version": "1",
  "project_version": "v1.1.0",
  "generated_at": "2026-08-31 00:55",
  "logical_files": 4293,
  "instances": 6721,
  "pending": 714,
  "invoices": 60,
  "official_docs": 96,
  "operations": 2920,
  "root_left": 0
}
```

- [ ] **Step 4: Implement the M20 page**

Create `pages/19_20_ding2026.py` with this structure and exact user-facing contracts:

```python
"""M20: read-only Ding2026 project showcase."""

from pathlib import Path
import sys

import streamlit as st


ROOT = Path(__file__).resolve().parents[1]
if str(ROOT) not in sys.path:
    sys.path.insert(0, str(ROOT))

from utils.ding2026_showcase import load_public_snapshot
from utils.ui_theme import render_home_link


st.set_page_config(page_title="Ding2026 文件治理系统", page_icon="🗂️", layout="wide")
render_home_link()

snapshot = load_public_snapshot(ROOT / "assets" / "ding2026_m20_snapshot.json")

st.title("M20 · Ding2026 文件治理系统")
st.markdown("**规则优先 · 手动分发 · 抽样验证 · 全程可撤销 · 永不自动删除**")
st.info("本页仅展示项目能力与脱敏快照；真实文件操作只在 Windows 本地项目中进行。")
st.markdown(
    "Ding2026 面向学院行政材料和 Google Drive 同步目录：它把散落材料先索引识别，"
    "再由用户手动触发分发，把不同周期的业务送入对应档案，并保留验证、撤销和备份链路。"
)

if snapshot:
    columns = st.columns(5)
    metrics = (
        ("已索引文件", snapshot["logical_files"]),
        ("待人工裁决", snapshot["pending"]),
        ("年度发票", snapshot["invoices"]),
        ("正式文件", snapshot["official_docs"]),
        ("可撤销记录", snapshot["operations"]),
    )
    for column, (label, value) in zip(columns, metrics):
        column.metric(label, f"{value:,}")
    st.caption(
        f"脱敏静态快照 · {snapshot['generated_at']} · {snapshot['project_version']} · "
        f"物理实例 {snapshot['instances']:,} · 根目录散落 {snapshot['root_left']}；不是实时数据。"
    )
else:
    st.warning("状态快照暂不可用；项目说明仍可正常阅读。")

problem_tab, ability_tab, usage_tab, archive_tab, limit_tab = st.tabs(
    ("解决什么问题", "它能做什么", "日常怎么用", "归档与安全", "当前限制")
)

with problem_tab:
    st.subheader("从散落文件变成可追溯档案")
    st.markdown(
        "材料常同时混有学期任务、年度工作、宣传照片、合作资料和长期制度。"
        "Ding2026 统一识别入口，但不把不同时间口径强行塞进同一目录；"
        "它还处理重复副本、命名不稳定、误归档和事后难追溯的问题。"
    )

with ability_tab:
    st.subheader("它能做什么")
    st.markdown(
        """
        - **索引与识别**：综合文件名、正文线索、日期和目录上下文。
        - **手动分发**：只有本地用户确认后才按规则归位，系统不自动移动。
        - **五条时间轴**：学期、工作年度、宣传年度、合作年度、长期知识库各归其位。
        - **中转裁决与学习**：不确定材料进入 `90_中转`，人工纠正继续沉淀为规则。
        - **验证与撤销**：保留操作记录，支持抽样验证、单条撤销和整批撤销。
        - **备份与防误删**：同名不同内容保留版本，数据库和配置保留本地备份。
        """
    )

with usage_tab:
    st.subheader("日常怎么用")
    st.markdown(
        "**放入材料 → 本地索引识别 → 用户点击手动分发 → 中转区人工裁决 → 抽样验证并完成归档**"
    )
    st.markdown(
        "首次使用时，在 Windows 本地克隆独立源码仓库、安装依赖、设置材料根目录、"
        "启动本地控制台并执行首次扫描。后续只需重复放入、分发、裁决和验证。"
    )

with archive_tab:
    st.subheader("五条时间轴，各归其位")
    st.markdown(
        """
        | 时间轴 | 典型内容 | 归档区域 |
        |---|---|---|
        | 学期 | 教学、会议、实习、学工、检查评估 | 学期工作档案 |
        | 工作年度 | 人事、竞赛、绩效、财务、述职、通知 | 年度工作档案 |
        | 宣传年度 | 新闻文档及配套照片 | 品牌新闻 |
        | 合作年度 | 合作单位资料与合同 | 产教融合 / 校企合作 |
        | 长期 | 制度、正式文件、专题资料 | 学院知识库 / 专题资料库 |
        """
    )
    st.markdown(
        "**安全边界：** 不自动删除；学期不猜；手工调整优先；重复点击会拦截；"
        "进入可删区的副本必须先验证归档位置存在相同内容。"
    )

with limit_tab:
    st.subheader("当前限制")
    st.markdown(
        "老式 `.doc/.xls/.ppt` 主要依赖文件名判断；当前没有 OCR；"
        "AI 预分类默认关闭，启用时需要在本地自行配置兼容接口。"
    )
```

Keep the implemented page equivalent to this contract. Minor spacing/style refinements are allowed only if they do not add operational controls or duplicate the source repository.

- [ ] **Step 5: Run page tests and verify GREEN**

Run the targeted pytest file. Expected: `7 passed`, the AppTest has no exceptions, and the policy test reports no forbidden controls/imports.

- [ ] **Step 6: Run syntax checks and commit the page**

```powershell
.\.venv\Scripts\python.exe -m py_compile utils\ding2026_showcase.py pages\19_20_ding2026.py
git add -- assets/ding2026_m20_snapshot.json pages/19_20_ding2026.py tests/test_ding2026_showcase_page.py
git diff --cached --check
git commit -m "feat: add read-only Ding2026 M20 page"
```

---

### Task 3: Homepage and Sidebar Registration

**Files:**
- Modify: `hello.py`
- Modify: `tests/test_home_page.py`
- Modify: `tests/test_sidebar_navigation_order.py`

**Interfaces:**
- Produces: `TOOLS` entry with code `M20`, page `pages/19_20_ding2026.py`, creation date `2026_08_31`, tag `文件治理`, and a non-blocked/non-locked state.
- Consumes: existing module-number ordering and nine-card pagination.

- [ ] **Step 1: Update navigation expectations before production metadata**

Change the relevant assertions to these literal outcomes:

```python
self.assertEqual("M20", tools[0]["code"])
self.assertEqual("pages/19_20_ding2026.py", tools[0]["page"])
self.assertEqual("M19", tools[1]["code"])
self.assertEqual(19, len(tools))

self.assertEqual(
    ["M20", "M19", "M18", "M17", "M15", "M14", "M13", "M11", "M10"],
    page1_codes,
)
self.assertEqual(
    ["M09", "M08", "M07", "M06", "M16", "M05", "M04", "M03", "M02"],
    page2_codes,
)
self.assertEqual(["M01"], page3_codes)
```

Update the sidebar assertion to:

```python
assert codes[:5] == ["M20", "M19", "M18", "M17", "M15"]
```

In the pagination test, define `page3_codes = [tool["code"] for tool in homepage_pages[2]]`.

- [ ] **Step 2: Run navigation tests and verify RED**

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_home_page.py tests\test_sidebar_navigation_order.py -q
```

Expected: assertions fail because `hello.py` still starts at M19 and has 18 tools.

- [ ] **Step 3: Add the M20 metadata entry**

Insert this dictionary at the start of `TOOLS` in `hello.py`:

```python
{
    "title": "Ding2026 文件治理系统",
    "desc": "展示学院行政文件如何被识别、手动分发、按五条时间轴归档，并通过验证、撤销和备份守住安全边界。",
    "tag": "文件治理",
    "created": "2026_08_31",
    "page": "pages/19_20_ding2026.py",
    "code": "M20",
    "accent": "cyan",
},
```

- [ ] **Step 4: Run navigation tests and verify GREEN**

Run the same navigation command. Expected: all tests in both files pass, with M20 first and the 19-card list split `9 / 9 / 1`.

- [ ] **Step 5: Commit navigation**

```powershell
git add -- hello.py tests/test_home_page.py tests/test_sidebar_navigation_order.py
git diff --cached --check
git commit -m "feat: register Ding2026 as M20"
```

---

### Task 4: Current Documentation and Change Log

**Files:**
- Modify: `README_ZH-CN.md`
- Modify: `README_EN.md`
- Modify: `README.md`
- Modify: `CHANGELOG_ZH-CN.md`
- Modify: `CHANGELOG_EN.md`
- Modify: `CHANGELOG.md`
- Modify: `tests/test_project_docs_and_setup.py`

**Interfaces:**
- Produces: one current documentation answer: nineteen active/preserved cards, including M19 Concept Fable Gallery and M20 Ding2026.
- Preserves: default English mirror equality for README and Change Log.

- [ ] **Step 1: Update documentation-governance assertions first**

Replace the stale module assertion with:

```python
def test_readmes_cover_current_m19_and_m20_modules():
    chinese = read("README_ZH-CN.md")
    english = read("README_EN.md")
    assert "十九个现存工具模块" in chinese
    assert "M19" in chinese and "M20" in chinese
    assert "Nineteen Current Tool Modules" in english
    assert "M19" in english and "M20" in english
```

Keep the existing mirror-equality test unchanged.

- [ ] **Step 2: Run the docs test and verify RED**

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_project_docs_and_setup.py -q
```

Expected: the current README still says seventeen and omits M19/M20.

- [ ] **Step 3: Align the Chinese and English READMEs**

Change the section titles to `十九个现存工具模块` and `Nineteen Current Tool Modules`. After the existing M18 section, add:

```markdown
### 19. 📜 概念寓言馆 (Concept Fable Gallery, M19)
- **场景**：把抽象概念转成容易记忆和检索的中文寓言。
- **功能**：线上页面只读浏览；确定性写入工具与展示层分离，避免网站直接修改馆藏。

### 20. 🗂️ Ding2026 文件治理系统 (M20)
- **场景**：展示学院行政文件治理项目如何解决材料散落、归档周期混杂和误操作风险。
- **功能**：说明索引识别、手动分发、五条时间轴、中转裁决、规则学习、验证撤销和备份边界。
- **线上边界**：仅展示脱敏静态快照，不读取数据库、业务目录或独立私有仓库，也不提供文件操作入口；真实操作只在 Windows 本地的 `ding2026-system` 项目中进行。
```

Add these English sections after M18:

```markdown
### 19. Concept Fable Gallery (M19)
- **Use case**: turning abstract concepts into memorable, searchable Chinese fables.
- **What it does**: the online page is read-only; deterministic catalog writing remains separate from the display layer so the website cannot directly change the collection.

### 20. Ding2026 File Governance System (M20)
- **Use case**: showing how the college-administration file-governance project addresses scattered materials, mixed archive cycles, and operational risk.
- **What it does**: explains indexing, manual distribution, five archive timelines, transfer-zone decisions, rule learning, verification, undo, and backup boundaries.
- **Online boundary**: the page displays only a desensitized static snapshot. It does not read a database, business directory, or the independent private repository, and it exposes no file-operation controls. Real operations remain in the local Windows `ding2026-system` project.
```

Then copy `README_EN.md` to `README.md` using the repository's normal file-copy mechanism so their bytes match exactly.

- [ ] **Step 4: Add the 2026-08-31 Change Log entry**

At the top of each named Change Log, record these facts in the corresponding language:

- M20 was added as a read-only Streamlit showcase under `pages/19_20_ding2026.py`.
- The independent private `ding2026-system` repository remains the operational source of truth.
- The page uses a strict ten-field static snapshot and contains no operational controls, local database/path access, or network/private-repository access.
- Existing README text lagged behind the already-live M19 card; the module count and M19/M20 descriptions were reconciled instead of preserving the stale test expectation.
- Verification used AppTest, targeted tests, full pytest, syntax checks, mirror checks, and remote/live checks; record the exact final pass count and any real warning from Task 5 rather than estimating it.
- Streamlit was not started locally, in accordance with repository rules.

Copy `CHANGELOG_EN.md` to `CHANGELOG.md` after the English entry is finalized.

- [ ] **Step 5: Run docs tests and verify GREEN**

Run the docs test. Expected: all tests pass and both default English mirrors match exactly.

- [ ] **Step 6: Commit current documentation**

```powershell
git add -- README_ZH-CN.md README_EN.md README.md CHANGELOG_ZH-CN.md CHANGELOG_EN.md CHANGELOG.md tests/test_project_docs_and_setup.py
git diff --cached --check
git commit -m "docs: document M20 read-only showcase"
```

---

### Task 5: Verification, Publication, and Clean Closeout

**Files:**
- Verify: all changed files from Tasks 1-4
- Verify unchanged: `E:\github\ding2026-system`
- Inspect only: repository residue, branches, worktrees, Markdown inventory, remote and deployed site

**Interfaces:**
- Produces: tested commits on `main`, pushed to `origin/main`, plus an evidence-based closeout report.
- Produces no deletion until the user reviews the cleanup preview and separately confirms deletion targets.

- [ ] **Step 1: Run the complete local verification gate**

```powershell
.\.venv\Scripts\python.exe -m pytest tests\test_ding2026_showcase_page.py tests\test_home_page.py tests\test_sidebar_navigation_order.py tests\test_project_docs_and_setup.py -q
.\.venv\Scripts\python.exe -m py_compile utils\ding2026_showcase.py pages\19_20_ding2026.py hello.py
.\.venv\Scripts\python.exe -m pytest -q
git diff --check
```

Read the complete output. If any test fails, diagnose the actual layer and fix it through a new failing regression test when production behavior is involved. Do not weaken tests or hide unrelated failures.

- [ ] **Step 2: Verify contracts and repository boundaries**

```powershell
.\.venv\Scripts\python.exe -c "from pathlib import Path; from utils.ding2026_showcase import load_public_snapshot; p=Path('assets/ding2026_m20_snapshot.json'); s=load_public_snapshot(p); assert s is not None and len(s)==10; print(s['project_version'], s['generated_at'], len(s))"
git status --short --branch
git log -6 --oneline
git -C E:\github\ding2026-system status --short --branch
git -C E:\github\ding2026-system diff --exit-code
```

Confirm the source repository has no modifications and the M20 snapshot has exactly ten approved fields.

- [ ] **Step 3: Update Change Log verification counts if the observed evidence differs**

Write only the exact pytest pass count, warnings, and limitations observed in Step 1. Re-run the docs mirror test, stage the Change Log correction, and amend only the documentation commit if it is still the most recent commit; otherwise create a narrow follow-up documentation commit.

- [ ] **Step 4: Fetch, reconcile safely, and push**

```powershell
git fetch origin
git rev-list --left-right --count HEAD...origin/main
```

If `origin/main` has new commits, inspect them and use a normal rebase without discarding local work. Then run the complete local verification gate again. When local history is current and verified:

```powershell
git push origin main
git rev-list --left-right --count HEAD...origin/main
```

Expected final divergence: `0  0`. Do not force-push.

- [ ] **Step 5: Verify the deployed Streamlit surface**

Open `https://yao-1.streamlit.app/` and the expected page path `https://yao-1.streamlit.app/20_ding2026`. Allow deployment time, then verify:

- the homepage shows M20 first;
- M20 loads without a Streamlit exception;
- purpose, capabilities, workflow, five timelines, safety boundary, and snapshot are visible;
- there are no file-operation controls;
- the snapshot is labeled static and non-real-time.

If deployment has not refreshed, report `pushed, deployment pending` and do not call it live verified.

- [ ] **Step 6: Run neat-freak full-path inventory and cleanup preview**

Read the required neat-freak references, mechanically enumerate repository files/Markdown/rules, and classify these fact surfaces: code, runtime, documents, rules, memory, and workspace. Inspect:

```powershell
git status --short --branch
git worktree list --porcelain
git branch -vv
git ls-files --others --exclude-standard
Get-ChildItem -LiteralPath . -Recurse -Force -File -ErrorAction SilentlyContinue |
    Where-Object { $_.FullName -notmatch '\\.git\\|\\.venv\\|node_modules|\\.next(?:-build)?\\' } |
    Select-Object -ExpandProperty FullName
```

List any obsolete plan, backup, temporary, generated, branch, or worktree candidates with the reason and recoverability. Preserve the review scene. The original instruction to “整理干净” does not authorize destructive clearing before this report.

- [ ] **Step 7: Report the verified state and request cleanup confirmation only if needed**

Report exact statuses:

- code: verified-current or changed-and-verified;
- runtime: live-verified or deployment-pending;
- documents: changed-and-verified;
- rules: verified-current;
- memory: out-of-scope/read-only unless the user separately requests an update;
- workspace: verified-current or pending-cleanup-confirmation.

If there are no material deletion candidates, report that the tracked worktree and remote are clean and close the task. If deletion candidates exist, stop and ask the user to confirm the exact list; delete nothing in this implementation turn.
