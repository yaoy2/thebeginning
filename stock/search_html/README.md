# 股票搜索在线副本

本目录保存 SerchHTML 的公开信息规则、最近快照和在线展示所需代码。本地 HTML 与采集服务仍保留在原项目中。

YaoYao M19 页面提供“采集最新热度”按钮，实时刷新当前会话中的每日热点、AI确认池、争议池和来源状态。线上刷新不改写 GitHub 中的 `data/latest.json`；任一来源受限会明确标记，全部失败或无可用内容时继续显示发布快照。
